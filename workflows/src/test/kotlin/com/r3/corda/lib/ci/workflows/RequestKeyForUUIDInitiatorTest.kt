package com.r3.corda.lib.ci.workflows

import org.junit.Test

/**
 * A placeholder test to keep Jenkins happy.
 *
 * The confidential identities tests have been moved to the token-sdk,
 * as keeping them here meant a circular dependency with token-sdk.
 */
class RequestKeyForUUIDInitiatorTest {
    @Test
    fun dummyTest() {
        // no-op
    }
}
